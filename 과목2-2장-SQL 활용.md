# SQL 활용
** :book: 과목2-2장 SQL 활용 **
---
## 51. 서브쿼리의 종류
    - 단일 행 서브쿼리 : 메인 쿼리로 하나의 값이 반환됨
    - 다중 행 서브 쿼리 : 메인 쿼리로 여러 개의 값이 반환됨
    - 다중 컬럼 서브 쿼리 : 메인 쿼리로 여러개의 컬럼 값이 반환됨.
## 52. 실행 결과가 다른 SQL문
    - CUBE와 ROLLUP의 함수의 인자가 2개이상인 경우는 결과가 달라질 수 있지만 1개일 때는 같다.
## 53. 
## 54. 집합 연산자에 대한 설명
    - UNION 연산자를 사용한 SQL은 각각의 집합에 GROUP BY절을 사용할 수 있다.
    - UNION 연산자를 사용한 SQL은 각각의 집합에 ORDER BY절을 사용할 수 없다.
    
## 55. SQL 실행 결과가 동일한 SQL
    - INTERSECT = 교집합
    - NOT EXISTS A -> A빼고 
---
---
## 56.
## 57. 
## 58. 아래의 실행 결과를 실행하는 SQL로 적절하지 않은 것
    - ORDER BY가 들어가면 사원번호 순서에 따라 급여를 누적해서 계산하고, 안들어가면 순서에 관계없이 각 부서의 급여가 누적되어 계산된다
## 59. 1:1, 양쪽 필수 관계
    - EXCEPT -> 공집합
    - UNION ALL -> 갯수가 2배가 된다
    - INTERSECT -> 연산 수행결과는 같다 
    
## 60. START WITH, CONNECTION BY, SIBLINGS 문제
    - C2가 NULL이 기준
    - C1은 자식컬럼 C2는 부모컬럼
    - SIBLING은 부모가 같은거끼리 정렬하기 떄문에 DESC 임으로 B와 C 순서가 바뀐다.
---
---
## 61.
## 62. 
## 63. 계층형 질의문
    - PRIOR 키워드는 CONNECT BY절뿐만 아니라 SELECT,WHERE 에서 사용 가능하다.
## 64. 
## 65. 
---
---
## 66.
## 67. DISTINCT A|| B 
    - 조건이 E = i인 DISTINCT A||B 는 a 1 b a, a 2 b a 로 두개다 유니크함으로 2건이다.
## 68. 서브 쿼리에 대한 설명
    - 서브쿼리가 복수 행 결과를 반한하는 경우에는 IN, ALL, ANY 연산자를 사용하여야 한다.
    - 다중 컬럼 서브쿼리는 현재 SQL에서는 지원되지 않는다.
## 69. PARTITION BY 실행 결과 종류
    - PERCENT_RANK : 행의 순서별로 백분율을 구하는 함수
    - DECENT_RANK : 순위를 구하는 함수로 동일한 순위를 하나의 등수로 취급한다.
    - CUME_DISK : 파티션 별 윈도우의 전체 건수에서 현재 행보다 작거나 같은 건수에 대한 누적 백분율을 구하는 함수이다 (무슨말인지 잘 모르겠다..)
    - RANK : 파티션 내의 ORDER BY에 의한 순위를 구하는 함수로, 동일한 순위에 대해서는 동일한 순위를 부여한다.
    - RATIO_TO_REPORT : 파티션 내의 주어진 칼럼 값의 합계에 대한 행별 백분율을 소수점으로 구하는 함수이다.
    - NTILE : 파티션 별 전체 건수를 주어진 인자로 N등분한 결과를 반환한다.
    - 문제의 결과를 보면 VAL1은 데이터의 VAL1 값을 GRP 별로 합계를 내고 비율을 구한 값임을 알 수 있고, VAL2은 전체 TABLE B 데이터의 VAL2 값으로 N등분한 값임을 알 수 있다.
    - 그러므로 RATIO_TO_REPORT, NTILE 함수를 써야 한다.
## 70. 동일한 결과를 출력하는 SQL중 적절하지 않은 것은?
    - 의 SQL은 약관 항목 중 단 하나라도 동의를 하지 않은 회원을 구하는 SQL이다. HAVING 절에서 동의 여부가 N인 데이터가 한 건이라도 존재하는 데이터를 추출한다. 
    - 3번의 회원 테이블과 동의 항목 테이블 간에 회원번호 칼럼이 연관 서브 쿼리로 처리되어야 정상적으로 처리할 수 있다.
---
---
## 71. SQL에 대한 설명
    - GROUP BY없이 HAVING을 사용해도 SQL 문장 오류가 발생하지 않는다.
## 72. 서브쿼리에 대한 설명
    - 단일 행 서브쿼리 비교 연산자는 다중 행 서브쿼리 비교에 사용 못하지만 반대는 가능하다.
    - 연관 서브쿼리는 외부 쿼리의 결과에 따라 동적으로 작동한다.
    
## 73. 
## 74. 
## 75. 뷰에 대한 설명
    - 뷰는 단지 정의만을 가지고 있으며, 실헹 시점에 질의를 재정의하여 작성한다.
    - 뷰는 테이블 구조가 변경되어도 응용 프로그램을 변경해 주지 않아도 된다.
---
---
## 76.
## 77. 
## 78. 
## 79. SQL에 대하여 가장 적절한 것
    - 부서별로 그룹바이를 하면 행이 여러개 나옴으로 WHERE 절 서브쿼리에는 여러개의 행이 올 수 없다.
## 80. 
---
---
## 81.
## 82. 
## 83. 
## 84. 윈도우 함수에 대한 설명
    - PARTITION BY 절과 GROUP BY절은 의미적으로 유사하다.
    - PARTITION BY 절이 없으면 전체 집합을 하나의 PARTION으로 정의한 것과 동일하다.
    - 윈도우 함수는 결과에 대한 함수처리이기 때문에 결과 건수가 줄어들지 않는다.
    - 윈도우 함수 적용범위는 PARTION을 넘을 수 없다.
## 85. 
---
---
## 86.
## 87. 
## 88. 
## 89. 
## 90. 
---
---
## 91. GRANT와 REVOKE에 대한 설명
    - 어떤 사용자가 WITH GRANT OPTION과 함꼐 권한을 받았다면 유무와 상관없이 다른 사용자에게 허가할 수 있다.
    - REVOKE문을 사용하여 권한을 취소하면 다른 사용자에게 허가했던 권한들도 연쇄적으로 취소된다.
    - REVOKE문을 사용하여 권한을 취소 할 때 그 권한을 허가한 사용자가 권한을 취소 할 수 있다.
## 92. 
## 93. 
## 94. 
## 95. 동일한 결과를 수행하는 SQL문
    - CUBE 그룹함수는 인수에 나열된 모든 칼럼의 가능한 조합에 대해서 소계를 함께 도출한다.
    - CUBE는 GROUPING SET 그룹함수를 이용하여 동일한 결과를 도출해낼수있다.
---
---
## 96.
## 97. 
## 98. 
